### **对象(Object)**
使用一个对象

~~~
	class Father {
    	public fn eat(){
        	print("eating")
        }
    }
    class Son extends Father{
    	public age int
    	public fn Son(age int){
        	this.age = age
        }
    }
    s := new Son(10)
    s.eat()
    print("age is "+ s.age)
	
~~~

