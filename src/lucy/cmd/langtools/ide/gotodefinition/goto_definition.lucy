import "lucy/cmd/langtools/compile/parser"
import "lucy/cmd/langtools/ide/utils"


public fn gotoDefinition(args []string) {
	f := new Flags()
    files , file , pos := f.parseArgs(args)
    lucyFiles := utils.readLucyFiles(files)
    for v := lucyFiles{
        if v.realPathName == file {
            
        }
    }
	imports := utils.parseImports()
    
}