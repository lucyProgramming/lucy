import "lucy/os"


const usage = `
    find definition of a element. 
       -file   file to be located
       -buffer-file the file been editing (optional)
       -line   which line (zero based)
       -column which column (zero based)
`

fn printUsageAndExit(code int ){
	os.stderr.println(usage)
	os.exit(code)
}







