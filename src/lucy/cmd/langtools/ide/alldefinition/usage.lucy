// all rights reserved , check the LICENSE file

import "lucy/os"

const usage = `
    find all definition of a directory. 
        -dir   which directory
`

fn printUsageAndExit(code int) {
    print(usage)
    os.exit(code)
}

