import "lucy/cmd/langtools/ide/gotodefinition"


public fn getHover (file string , pos int , bs []byte) ->(s string = "") {
    p := gotodefinition.findDefinition(file , "" , pos , bs)
    if p == null {
        return  
    }
    // if p.locateDefinitionIs == null {
    //     return 
    // }
    // s = new MakeHover(p.locateDefinitionIs).make() 
}



public fn main(args []string) {
	 
}