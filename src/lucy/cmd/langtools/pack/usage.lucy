// all rights reserved , check the LICENSE file

import "lucy/os"

const usage = `
    -o                 the outout file name
    -package-name      the package name you want pacakge 
    -manifest          the jar manifest file 
`

fn printUsageAndExit(code int) {
    print(usage)
    os.exit(code)
}
