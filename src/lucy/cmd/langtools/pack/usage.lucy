// all rights reserved , check the LICENSE file

import "lucy/os"

const usage = `
    -o                 the outout file name
    -package-name      the "package name" you want package 
    -manifest          the optional jar manifest file 
    -v                 verbose
`


fn printUsageAndExit(code int) {
    print(usage)
    os.exit(code)
}
