package string;

import "java/lang" as javalang;

public class ArrayToString{
	public static fn toString(arr []bool)->(result string){
		result = "[";
		length := arr.size();
		for i := 0;i<length;i++{
			result += javalang.String.valueOf(arr[i]);
			if i != length -1{
				result += " "
			}
		}
		result += "]"
	}
	public static fn toString(arr []int)->(result string){
		result = "[";
		length := arr.size();
		for i := 0;i<length;i++{
			result += javalang.String.valueOf(arr[i]);
			if i != length -1{
				result += " "
			}
		}
		result += "]"
	}
	public static fn toString(arr []long)->(result string){
		result = "[";
		length := arr.size();
		for i := 0;i<length;i++{
			result += javalang.String.valueOf(arr[i]);
			if i != length -1{
				result += " "
			}
		}
		result += "]"
	}
	public static fn toString(arr []float)->(result string){
		result = "[";
		length := arr.size();
		for i := 0;i<length;i++{
			result += javalang.String.valueOf(arr[i]);
			if i != length -1{
				result += " "
			}
		}
		result += "]"
	}
	public static fn toString(arr []double)->(result string){
		result = "[";
		length := arr.size();
		for i := 0;i<length;i++{
			result += javalang.String.valueOf(arr[i]);
			if i != length -1{
				result += " "
			}
		}
		result += "]"
	}
	public static fn toString(arr []javalang.Object)->(result string){
		result = "[";
		length := arr.size();
		for i := 0;i<length;i++{
			result += javalang.String.valueOf(arr[i]);
			if i != length -1{
				result += " "
			}
		}
		result += "]"
	}	
}






