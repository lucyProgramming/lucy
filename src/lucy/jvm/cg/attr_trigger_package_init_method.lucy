public class AttributeTriggerPackageInitMethod {
    fn toAttributeInfo(c Class) -> (ret AttributeInfo) {
        info := new AttributeInfo()
        info.nameIndex = c.insertUtf8Const(AttributeNameLucyTriggerPackageInitMethod)
        return info
    }
}